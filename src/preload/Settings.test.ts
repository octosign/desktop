describe('Settings', () => {
  it.todo('Can create schema from the settings def');

  it.todo('Can load settings from the config file');

  it.todo('Can save settings to the config file');

  it.todo('Can restore settings in the config to their defaults');
});
